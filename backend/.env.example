#
# Variáveis de Ambiente para a Aplicação
#
# Crie uma cópia deste arquivo chamada .env e preencha os valores.
# O arquivo .env local NUNCA deve ser comitado ao Git.
#

# ===============================================
# Configurações Gerais do Projeto e CORS
# ===============================================

# URL permitida para acesso CORS (Geralmente a URL de desenvolvimento do Frontend)
CORS_ORIGIN=http://localhost:5173

# ===============================================
# Segurança
# ===============================================

# CHAVE SECRETA: Necessária para assinar tokens JWT (insira um valor seguro no .env)
JWT_SECRET=

# ===============================================
# Chave da API OpenRouter
# ===============================================

# Chave de acesso à API OpenRouter (O valor REAL deve ser inserido apenas no .env local)
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===============================================
# Conexões com Serviços Docker
# ===============================================

# String de Conexão com o MongoDB.
# Nota: O host 'mongo' é usado quando rodando via Docker Compose.
MONGO_URI="mongodb://mongo:27017/documentosDB"

# Nome do Bucket do MinIO usado para armazenamento de arquivos
MINIO_BUCKET_NAME=categorizar-docs-grupo1


# ===============================================
# Configurações do MinIO
# ===============================================

# Host/Endpoint do serviço MinIO
MINIO_ENDPOINT=minio

# Porta do serviço MinIO
MINIO_PORT=9000

# Define se deve usar conexão segura (SSL/HTTPS) com o MinIO
MINIO_USE_SSL=false

# Chave de Acesso (Access Key) do MinIO (Valor de desenvolvimento não-secreto)
MINIO_ACCESS_KEY=minioadmin

# Chave Secreta (Secret Key) do MinIO (Valor de desenvolvimento não-secreto)
MINIO_SECRET_KEY=minioadmin